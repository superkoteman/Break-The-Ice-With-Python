while True:
    try:
        n = int(input("Please, enter a number greater than 1: "))
        if n >= 1:
            dict = {n:n**2 for n in range(1, n + 1)}
            print(dict)
            break
        else:
            continue
    except:
        continue
